# rabota-11-14-16-17
﻿Работа с массивами в C
 
## 📋 Задания к лаб. 11
1. Ввод размера массива с клавиатуры
2. Заполнение массива с консоли
3. Преобразование массива(отбросить дробную часть у всех элементов массива)
4. Печать массива
5. Найти cреднее значение положительных элементов массива,
расположенных по нечетным индексам.

## 📋 Задания к лаб. 14

1. Заполнение массива значениями из функции func(x)
2. Печать массива
3. Вычисление суммы элементов массива
4. Поиск порядкового номера максимального значения из элементов, начиная с индекса ka
5. Определение количества элементов, находящийся между двумя соседними значениями
равными А.


## 📋 Задания к лаб. 16
1. Выделение памяти под массив
2. Заполнение массива значениями из функции func(x)
3. Обработка элементов массива
4. Печать массива
5. Перепись массива
6. Заполнение массива случайными элементами от [-1;1]
7. Удаление всех элементов меньших среднего арифметического 
8. Вставка значения -999 перед первым отрицательным.

## 📋 Задания к лаб. 17
Реализовать функции сортировки массива:
1. Пузырьковой сортировки - void sort_bubble(int* ptr_array, int n);
2. Сортировки простым выбором - void sort_select(int* ptr_array, int n);
3. Сортировки вставками - void sort_insert(int* ptr_array, int n);
4. Шейкерная сортировка - void sort_cocktail(int* arr, int size);
5. Провести эксперимент, сравнивающий скорость проведения каждой из сортировок.
Разработать таблицу с результатами эксперимента.

## 📁 Структура проекта

lab-arrays/

├── src/

│   ├── arrays.h

│   ├── arrays.c

│   └── app.c

├── Makefile

└── README.md


## 🔧 Реализованные функции

### 1. `double func(double x)`
- **Назначение**: Вычисление значения функции y = x³ − 4x² + 2  
- **Параметры**:  
  - `x` — аргумент функции  
- **Возвращает**: значение функции в точке x

### 2. `double* full_elements_func(double* ptr_array, int n)`
- **Назначение**: Заполнение массива значениями, получаемыми из функции func()  
- **Параметры**:  
  - `ptr_array` — указатель на массив  
  - `n` — размер массива  
- **Возвращает**: указатель на заполненный массив

### 3. `int put_elements(double* ptr_array, int n)`
- **Назначение**: Печать элементов массива типа double на экран  
- **Параметры**:  
  - `ptr_array` — указатель на массив double  
  - `n` — количество элементов для вывода  
- **Возвращает**: количество выведенных элементов (n)

### 4. `double* calc_elements(double* ptr_array, int n)`
- **Назначение**: Отбрасывание дробной части у всех элементов массива  
- **Параметры**:  
  - `ptr_array` — указатель на массив double  
  - `n` — количество элементов для обработки  
- **Возвращает**: указатель на изменённый массив

### 5. `double sum_elements(double* ptr_array, int begin, int end)`
- **Назначение**: Вычисление суммы элементов массива на заданном интервале [begin..end]  
- **Параметры**:  
  - `ptr_array` — указатель на массив double  
  - `begin` — начальный индекс (включительно)  
  - `end` — конечный индекс (включительно)  
- **Возвращает**: сумма элементов

### 6. `int find_element(double* ptr_array, int n, double element)`
- **Назначение**: Поиск первого вхождения заданного элемента в массиве  
- **Параметры**:  
  - `ptr_array` — указатель на массив double  
  - `n` — количество элементов  
  - `element` — искомое значение  
- **Возвращает**: индекс первого совпадения или -1, если элемент не найден

### 7. `double* full_elements_console(double* ptr_array, int n)`
- **Назначение**: Заполнение массива значениями, вводимыми пользователем с клавиатуры  
- **Параметры**:  
  - `ptr_array` — указатель на массив  
  - `n` — размер массива  
- **Возвращает**: указатель на заполненный массив

### 8. `double* fill_random_minus1_to1(double* ptr_array, int n)`
- **Назначение**: Заполнение массива случайными вещественными числами из диапазона [-1, 1]  
- **Параметры**:  
  - `ptr_array` — указатель на массив  
  - `n` — размер массива  
- **Возвращает**: указатель на заполненный массив

### 9. `double average_positive_odd_indices(double* ptr_array, int n)`
- **Назначение**: Вычисление среднего арифметического положительных элементов на нечётных индексах  
- **Параметры**:  
  - `ptr_array` — указатель на массив double  
  - `n` — размер массива  
- **Возвращает**: среднее значение или 0.0, если подходящих элементов нет

### 10. `int find_max_from_index(double* ptr_array, int n, int ka)`
- **Назначение**: Поиск индекса максимального элемента, начиная с заданного индекса ka  
- **Параметры**:  
  - `ptr_array` — указатель на массив  
  - `n` — размер массива  
  - `ka` — начальный индекс поиска  
- **Возвращает**: индекс максимального элемента или -1 при ошибке

### 11. `int count_between_two_a(double* ptr_array, int n, double a)`
- **Назначение**: Подсчёт количества элементов между двумя соседними вхождениями значения A  
- **Параметры**:  
  - `ptr_array` — указатель на массив  
  - `n` — размер массива  
  - `a` — искомое значение  
- **Возвращает**: количество элементов между двумя первыми вхождениями A или 0

### 12. `double* delete_less_than_avg(double* arr, int* size, double avg)`
- **Назначение**: Удаление всех элементов, меньших заданного среднего значения  
- **Параметры**:  
  - `arr` — указатель на массив  
  - `size` — указатель на текущее количество элементов (будет изменён)  
  - `avg` — пороговое значение (среднее)  
- **Возвращает**: новый указатель на массив или NULL, если элементов не осталось

### 13. `int find_first_negative(double* ptr_array, int n)`
- **Назначение**: Поиск индекса первого отрицательного элемента в массиве  
- **Параметры**:  
  - `ptr_array` — указатель на массив  
  - `n` — размер массива  
- **Возвращает**: индекс первого отрицательного элемента или -1

### 14. `double* insert_value_at(double* arr, int* size, double value, int pos)`
- **Назначение**: Вставка заданного значения в массив на указанную позицию  
- **Параметры**:  
  - `arr` — указатель на массив  
  - `size` — указатель на текущее количество элементов  
  - `value` — вставляемое значение (-999.0)  
  - `pos` — позиция вставки  
- **Возвращает**: новый указатель на расширенный массив

### 15. `int create_d_from_a_b(double* a, int n, double* b, int m, double** d)`
- **Назначение**: Создание нового массива D: элементы массива A до минимального (не включая), затем элементы массива B без минимального и максимального  
- **Параметры**:  
  - `a` — указатель на массив A  
  - `n` — размер массива A  
  - `b` — указатель на массив B  
  - `m` — размер массива B  
  - `d` — указатель на указатель для результата (выделяется внутри)  
- **Возвращает**: размер созданного массива D

### 16. `int* full_array(int* ptr_array, int n)`
- **Назначение**: Заполнение массива целых чисел случайными значениями в диапазоне [-1000, 1000]  
- **Параметры**:  
  - `ptr_array` — указатель на массив целых чисел  
  - `n` — размер массива  
- **Возвращает**: указатель на заполненный массив

### 17. `int put_elements_int(int* ptr_array, int n)`
- **Назначение**: Печать элементов массива целых чисел на экран  
- **Параметры**:  
  - `ptr_array` — указатель на массив int  
  - `n` — количество элементов  
- **Возвращает**: количество выведенных элементов

### 18. `void sort_bubble(int* ptr_array, int n)`
- **Назначение**: Пузырьковая сортировка массива целых чисел по возрастанию  
- **Параметры**:  
  - `ptr_array` — указатель на массив целых чисел  
  - `n` — количество элементов  
- **Возвращает**: 0 при успешном выполнении

### 19. `void sort_select(int* ptr_array, int n)`
- **Назначение**: Сортировка выбором массива целых чисел по возрастанию  
- **Параметры**:  
  - `ptr_array` — указатель на массив целых чисел  
  - `n` — количество элементов  
- **Возвращает**: 0 при успешном выполнении

### 20. `void sort_insert(int* ptr_array, int n)`
- **Назначение**: Сортировка вставками массива целых чисел по возрастанию  
- **Параметры**:  
  - `ptr_array` — указатель на массив целых чисел  
  - `n` — количество элементов  
- **Возвращает**: 0 при успешном выполнении

### 21. `void sort_cocktail(int* arr, int size)`
- **Назначение**: Шейкерная (коктейльная) сортировка массива целых чисел по возрастанию  
- **Параметры**:  
  - `arr` — указатель на массив целых чисел  
  - `size` — количество элементов  
- **Возвращает**: 0 при успешном выполнении
